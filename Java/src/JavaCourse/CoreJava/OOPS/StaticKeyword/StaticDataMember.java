package src.JavaCourse.CoreJava.OOPS.StaticKeyword;
// In this snippet, we will discuss Static Data Member in Java
// Static data members are also known as class variables. These variables are declared with the static keyword.
// These variables are common to all the objects of the class. They are initialized only once at the start of the execution.
// These variables are created when the program starts and destroyed when the program stops.
// Static data members are accessed using the class name and not the object name.
// Static data members are used to refer to the common property of all objects (that is not unique for each object).
// Static data members are used to refer to the common property of all objects (that is not unique for each object).
public class StaticDataMember {
    public static void main(String[] args) {

    }
}
