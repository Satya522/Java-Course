package JavaCourse.DSA.Arrays.TwoD;

public class AboutMatrix {
    public static void main(String[] args) {
        System.out.println("This is all about 2D array in java");
        /*
        A 2D array, also known as a matrix, is essentially an array of arrays. It is a data structure that can be
        visualized as a grid (or a table) with rows and columns. Each cell in this grid can store a value, and can be
        accessed by specifying two indices: one for the row and one for the column
         */
    }
}
